#ifndef COMPUTE_REWARD_H
#define COMPUTE_REWARD_H

// Menghitung reward (gain) dari base score, memperbarui saldo uang lewat
// updateMoney(), lalu mencetak log [REWARD].
// Return: nilai gain
int computeReward(int baseScore, int &money);

#endif // COMPUTE_REWARD_H