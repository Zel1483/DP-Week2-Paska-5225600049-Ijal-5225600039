#include "compute_base_score.h"
#include <iostream>

int computeBaseScore(int input) {
    int baseScore = input; // aturan skor sederhana, silakan dikembangkan
    std::cout << "[SCORE] base score: " << baseScore << std::endl;
    return baseScore;
}